---
import { sidebarLayoutConfig } from '../config';

// 从配置中获取个人资料信息
const leftSidebarConfig = sidebarLayoutConfig.find(sidebar => sidebar.position === 'left');
const profileConfig = leftSidebarConfig?.components.find(component => component.name === 'Profile')?.config || {};

const {
  avatar = profileConfig.avatar || 'http://q.qlogo.cn/headimg_dl?dst_uin=483165474&spec=640&img_type=jpg',
  name = profileConfig.name || 'XingGreen',
  quote = profileConfig.bio || '人生如梦，不期而遇',
  socialLinks = profileConfig.socialLinks || []
} = Astro.props;
---

<div class="author-profile">
  <div class="profile-card">
    <div class="avatar-container">
      <img src={avatar} alt={`${name}的头像`} class="author-avatar" />
    </div>
    
    <h2 class="author-name">{name}</h2>
    <p class="author-quote">{quote}</p>
    
    <div class="social-links">
      {socialLinks.map((link, index) => (
        <a 
          key={index} 
          href={link.url} 
          class="social-link" 
          aria-label={`访问我的${link.name}主页`}
          target="_blank"
          rel="noopener noreferrer"
        >
          <span class="material-symbols-outlined">{link.icon}</span>
          <span>{link.name}</span>
        </a>
      ))}
    </div>
  </div>
</div>

<style>
.author-profile {
  margin-bottom: 2rem;
}

.profile-card {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: 12px;
  padding: 2rem;
  text-align: center;
  backdrop-filter: blur(10px);
  box-shadow: 0 4px 6px var(--shadow);
}

.avatar-container {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  overflow: hidden;
  margin: 0 auto 1.5rem;
  border: 3px solid var(--accent-color);
}

.author-avatar {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.author-name {
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  color: var(--text-primary);
}

.author-quote {
  font-size: 0.95rem;
  color: var(--text-secondary);
  margin-bottom: 1.5rem;
  font-style: italic;
}

.social-links {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.social-link {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: 8px;
  color: var(--text-primary);
  text-decoration: none;
  transition: all 0.3s ease;
  font-size: 0.85rem;
}

.social-link:hover {
  background: var(--accent-color);
  color: white;
  transform: translateY(-2px);
  box-shadow: 0 4px 8px var(--shadow);
}

.social-link span.material-symbols-outlined {
  font-size: 1.2rem;
}
</style>